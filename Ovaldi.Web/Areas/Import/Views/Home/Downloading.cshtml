@{
    ViewBag.Title = "Downloading";
    Layout = "~/Views/Shared/Blank.cshtml";
}

<h2>Download progress</h2>

<div class="container">
    <ul id="discussion"></ul>
</div>
<!--Script references. -->
<!--Reference the SignalR library. -->
<script src="~/Scripts/jquery.signalR-2.1.2.min.js"></script>
<!--Reference the autogenerated SignalR hub script. -->
<script src="~/signalr/hubs"></script>
<!--Add script to update the page and send messages.-->
<script type="text/javascript">
    $(function () {
        // Declare a proxy to reference the hub.
        var chat = $.connection.chatHub;
        // Create a function that the hub can call to broadcast messages.
        chat.client.broadcastMessage = function (name, message) {
            // Html encode display name and message.
            var encodedName = $('<div />').text(name).html();
            var encodedMsg = $('<div />').text(message).html();
            // Add the message to the page.
            $('#discussion').append('<li><strong>' + encodedName
                + '</strong>:&nbsp;&nbsp;' + encodedMsg + '</li>');
        };
        chat.client.onPageDownloaded = function (result) {
            $('#discussion').append('<li>' + result.DownloadedPages + ':&nbsp;&nbsp;<strong>' + result.CurrentPage.Url
                 + '</strong>:&nbsp;&nbsp;Current level:' + result.CurrentPage.Level + '&nbsp;&nbsp;Remain pages:' + result.Remains + '</li>');
        };
        chat.client.onDownloadCompleted = function (result) {
            $('#discussion').append('<li><strong>' + 'Downloaded pages'
               + '</strong>:&nbsp;&nbsp;' + result.length + '</li>');
            chat.client.stop();
        };
        // Start the connection.
        $.connection.hub.start().done(function () {
            console.log($.connection.hub.id);
        });
    });
</script>
